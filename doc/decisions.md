# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | arch | Backend language and runtime | Python 3.13 + FastAPI + Pydantic v2 + SQLAlchemy 2.0 async | User has deep Python + FastAPI + AWS experience. FastAPI's OpenAPI schema gives free, accurate TS client generation across the language boundary. Earlier Node/TS pick was reversed mid-discussion for familiarity and velocity. | No | human |
| D002 | M001 | arch | API compute target | AWS ECS Fargate + ALB via CDK | App Runner closed to new customers in 2025. Lambda cold starts hurt the agent tool-callback loop and the drip-cadence worker. ECS Fargate is stable, well-documented, fits user AWS expertise, and CDK provides real composition for infra-as-code. | Yes — if operational burden outweighs control | human |
| D003 | M001 | arch | Data plane and migration tooling | Supabase (Postgres + Auth + Storage); Supabase CLI owns migrations (raw SQL); SQLAlchemy as query layer only; no Alembic | User explicitly chose Supabase CLI migrations. Keeps Supabase dashboard, realtime, and RLS coherent. Schema-first workflow is worth losing alembic autogenerate. | No | human |
| D004 | M001 | arch | Agent runtime and state model | Amazon Bedrock AgentCore (Claude); hybrid state — short-term scratchpad in AgentCore Memory, durable turn log + graph + Dossier in Postgres at turn boundaries | AgentCore is built for long-running, tool-using, human-in-the-loop agents and provides session/memory/hook primitives we'd otherwise hand-roll. Hybrid state keeps Command Center reads cheap (Postgres) while leveraging AgentCore's in-session primitives. Pure AgentCore ties product to Bedrock memory; pure Postgres wastes what AgentCore gives for free. | Yes — if AgentCore primitives prove insufficient or AWS investment wavers | collaborative |
| D005 | M001/S02 | data | Itinerary graph storage | Postgres-native relational: nodes, edges, node_history, edge_history with enums for status + edge type, nullable parent_subgraph_id for subgraphs, append-only history, recursive CTEs for traversal, source + source_id on inventory-sourced nodes | Itineraries are 30–80 nodes — trivially handled by Postgres with recursive CTEs. Append-only history is the audit trail advisors and agent both read. Provenance fields keep the graph portable across providers. JSONB blob is terrible for edges/history/partial edits; dedicated graph DB is overkill. | No | human |
| D006 | M001/S02 | pattern | Inventory provider abstraction | Source-agnostic agent tools (search_inventory, get_inventory_detail) dispatching through a provider registry to per-source adapters that normalize to a common InventoryItem shape | OV is inventory source #1 of many (Duffel, Ratehawk, editorial partners to follow). Baking source knowledge into the agent means every new provider becomes an agent-prompt refactor. Day-one abstraction is cheaper than later migration. | No | human |
| D007 | M001/S04 | api | Agent ↔ API tool boundary | Thin primitives for most reads/writes (search_inventory, get_inventory_detail, get_dossier, propose_node, update_node_status, list_alternatives); one thick composite (assemble_initial_draft) for the stable, round-trip-expensive assembly orchestration. All graph reads pass through FastAPI — no direct agent Postgres access. | Graph semantics (subgraphs, status transitions, history) are too complex to reassemble in agent prompts or raw SELECTs. One source of graph semantics prevents drift. Thin tools let us iterate without API churn. | Yes — add thick composites if hot paths emerge | collaborative |
| D008 | M001/S08 | pattern | Approval gate scope | Gate applies to draft itinerary only. Mood-board cards and onboarding chat responses surface live (no gate). Full assembled draft held in draft state, visible only to advisor; client sees final itinerary only post-approval. Advisor edits lock the graph for agent writes; agent queues proposals until release. | Literal "advisor reviews all AI output" would kill conversation liveness. Advisor's craft-guarantee role belongs at the itinerary moment, not every turn. Lock-and-queue prevents last-writer-wins on concurrent advisor/agent writes. | No | human |
| D009 | M001/S04 | pattern | Onboarding to assembly transition | Explicit signals (client names a destination, asks "what would you suggest") plus agent self-rubric on Dossier coverage. Transition is a distinct phase change reflected in UI (cards start appearing). | Pure self-judgment drifts too fast or too slow. Advisor-gated transition would kill mood-board responsiveness. Signal-triggered with rubric grounding is the balance. v1 rubric ships in M001; v2 emerges from watching real conversations. | Yes — rubric evolves from observation | human |
| D010 | M001/S06 | convention | Atmospheric morph system | Hand-tuned named-mood palettes (glacial, ember, verdant, etc.); curated image library with tagged Unsplash fallback; morph triggers on phase shifts (every 3–5 meaningful exchanges, not per message); 60fps transitions with 3–5s fades; WCAG AA contrast preserved across all palettes; the conversation surface holding agent prose stays stable-color while the full-bleed frame around it morphs. | Algorithmic palette extraction produces muddy tints. Raw Unsplash queries return tourist snapshots. Curated-first preserves craft feel. User explicitly emphasized conversation-surface stability — morph is weather around stable correspondence. | No — the morph/stability split is a craft decision, not a technical one | human |
| D011 | M001 | arch | Web stack and hosting | Next.js 15 + React 19 + Tailwind + shadcn on Vercel; TS API client generated from FastAPI OpenAPI schema | User wanted to keep Vercel DX where the team iterates most. Next.js 15 is the latest stable with App Router + React 19 + Turbopack dev. OpenAPI-generated client eliminates hand-maintained types across the Python/TS boundary. | No | human |
| D012 | M001 | convention | Monorepo layout | pnpm workspaces + Turborepo: apps/api (FastAPI), apps/web (Next.js 15), packages/api-client (OpenAPI-generated TS), infra/cdk (AWS CDK) | Monorepo shares the generated API client cleanly and keeps infra definitions co-located with the code they provision. Turborepo handles task orchestration (build, lint, test) across heterogeneous runtimes (Python + TS). | Yes — split later if workspace weight becomes painful | human |
| D013 | M001 | convention | Error handling and failure disclosure | Silent retry with exponential backoff on transient failures. If recovery fails, surface an honest crafted message ("your concierge is stepping away for a moment") — never a stack trace, never a broken UI. All failures log to CloudWatch with session/turn/actor/input context. | A luxury product must never feel fragile. Silent recovery when possible; honest disclosure when not. Stack traces or broken UI break the craft line immediately. | No | human |
| D014 | M001/S01/T05 | pattern | Invite single-use enforcement strategy | DB-enforced atomic consume: UPDATE invites SET consumed_at=now() WHERE code=:c AND consumed_at IS NULL RETURNING code. Empty RETURNING maps to already_consumed. Rollback on upstream Supabase failure. | Application-level SELECT-then-UPDATE creates a time-of-check/time-of-use gap letting concurrent requests double-redeem. Postgres gives us atomic test-and-set via the WHERE consumed_at IS NULL predicate — the DB is the source of truth for single-use. Rolling back on upstream failure keeps UX tolerant of transient Supabase hiccups without letting the invite silently burn. | Yes — swap for pg advisory lock or a queued worker if multi-seat invite semantics are ever needed. | agent |
| D015 | M001/S01/T05 | security | Invite redemption error disclosure | Collapse unknown-code and wrong-email into the same 404 {detail: "invite_not_redeemable"} response. Already-consumed returns 409. Upstream-unavailable returns 502. | Distinct responses for unknown-code vs wrong-email let an attacker enumerate which codes exist by probing with any email: a 404 says not found, a 409 or "wrong email" would confirm the code is real. Collapsing both into the same 404 makes enumeration require an exact email match per probe. Already-consumed gets its own 409 because the information leak is symmetric — the code holder already knows it was consumed. | Yes — revisit if product wants a softer UX for the wrong-email case and accepts the enumeration surface (e.g. behind a CAPTCHA). | agent |
| D016 | M001/S01/T07 | library | OpenAPI → TypeScript client generator for packages/api-client | openapi-typescript-codegen 0.30.0 with --client fetch --useOptions --useUnionTypes; generator spawned from a shell script that boots apps/api via `uv run uvicorn`, curls /openapi.json, then shuts the API down before regenerating src/generated/. | The plan explicitly named openapi-typescript-codegen, and it still produces a usable fetch client with a Services layer (OvBlackApiClient.auth.redeemInviteEndpointAuthRedeemInvitePost) plus typed models (RedeemInviteRequest, ApiError). @hey-api/openapi-ts is the official successor, but switching now would diverge from the slice plan without a forcing function. --useOptions gives named-parameter ergonomics; --useUnionTypes emits string-literal unions from enums (matches our R017-adjacent narrow-shape convention). The boot-curl-shutdown script keeps regeneration a one-command affair and avoids relying on a running API. | Yes — migrate to @hey-api/openapi-ts (the official successor) the next time this surface needs a feature openapi-typescript-codegen has stopped shipping (auth helpers, fine-grained tree-shaking, OpenAPI 3.1 literal support, etc.). | agent |
| D017 | M001/S01/T07 follow-up | library | Supersede D016 — migrate packages/api-client codegen from openapi-typescript-codegen to @hey-api/openapi-ts | @hey-api/openapi-ts 0.96.1 (devDep only) driven by packages/api-client/openapi-ts.config.ts using the @hey-api/client-fetch + @hey-api/sdk + @hey-api/typescript plugins. The boot/curl/shutdown script from D016 is unchanged; only the codegen invocation swapped to `npx openapi-ts`. The generated code is self-contained (bundles its fetch runtime), so no @hey-api/client-fetch runtime dep is needed. The src/index.ts wrapper surface (createApiClient, redeemInvite, RedeemInviteResult, RedeemInviteDetail) is preserved so apps/web is untouched. packages/api-client/tsconfig.json gets a scoped override (exactOptionalPropertyTypes: false, adds DOM.Iterable lib) to accept hey-api's generated-code style without relaxing the repo-wide strict bar. | User requested the switch directly. hey-api is the actively maintained successor to openapi-typescript-codegen, with better OpenAPI 3.1 support and a plugin architecture. Doing the swap now (one file of hand-written surface area + one config file) is dramatically cheaper than after T09/CI wires codegen into the deploy pipeline, and keeps the wrapper boundary intact so downstream tasks and apps/web see no API change. The scoped tsconfig relaxation is contained to this one package — the hand-written wrapper is a single file, and the generated tree is gitignored and treated as a dependency. | Yes — revisit if hey-api stops shipping, changes its plugin contract in breaking ways, or the generated-code style drifts from our ergonomics needs. | human |
| D018 | M001/S01/T08 | pattern | How infra/cdk consumes the cross-account NetworkStack VPC | Import the VPC via `Vpc.fromVpcAttributes` driven by per-env CDK context (vpcId, availabilityZones, publicSubnetIds, privateSubnetIds) with dummy fallbacks (vpc-00000000 + placeholder subnet IDs) when context is empty. NetworkStack lives in a separate Control-Tower-governed account/repo and is NOT modeled in this CDK app; staging and prod each carry their own `ov-black:envs.<env>` block in `cdk.json` that operators populate before deploy. | `Vpc.fromLookup` would need live AWS credentials at synth time, which breaks `cdk synth` from a clean checkout and from CI without an assumed SSO role. `fromVpcAttributes` keeps synthesis hermetic and deterministic — CI and local devs can run `pnpm cdk synth` without being logged into AWS. The dummy fallbacks are an intentional footgun shield: synth succeeds silently, but anyone running `cdk deploy` against a real account without populating real vpcId/subnets will fail fast at CloudFormation, not halfway through provisioning. Control Tower adds separate staging/prod accounts, so per-env context blocks are the right seam — not a single SSM param shared across accounts. | Yes — if we ever consolidate the NetworkStack into this monorepo, switch to a direct CfnOutput/Fn.importValue chain or a same-app cross-stack ref. | agent |
| D019 | M001/S01/T08 | convention | infra/cdk tsconfig.json does NOT extend tsconfig.base.json | The CDK workspace owns a self-contained tsconfig with `module: commonjs`, `moduleResolution: node`, no `verbatimModuleSyntax`, no `isolatedModules`, no `noUncheckedIndexedAccess`. It keeps strict + noImplicitReturns + noFallthroughCasesInSwitch from the house style but explicitly diverges on module system and unused-locals policy. | See above. | Yes — revisit if `aws-cdk` ships first-class ESM support or if the repo migrates off ts-node (e.g. to tsx or a compiled bundle). | agent |
| D020 | Adopted in M001/S02/T01 when writing the test_saenum_create_type_is_false_for_all_graph_enums regression guard | pattern | How to bind SQLAlchemy to Postgres-owned enums so the create_type=False guarantee is testable | Use sqlalchemy.dialects.postgresql.ENUM (PGEnum) for new Postgres-native enums, not the generic sqlalchemy.Enum (SAEnum) | The generic SAEnum silently drops the create_type kwarg (it's Postgres-dialect-specific) so a regression test like `assert enum.create_type is False` raises AttributeError — the flag only materializes when the DB dialect impl is resolved. postgresql.ENUM stores create_type as a real instance attribute, which makes the D003 invariant (Supabase CLI owns DDL, SQLAlchemy must never emit CREATE TYPE) directly assertable from a pure-Python test. Functional behavior is identical; only observability differs. Existing SAEnum sites in profile.py/invite.py are left as-is to keep S01 untouched. | Yes — if a future change wants to unify, migrate profile.py/invite.py to PGEnum in a dedicated refactor slice | agent |
| D021 | M001/S05/T01+T02 | security / auth | How the "client themself" branch of POST /sessions resolves when S03 only seeds clients.email (auth_user_id IS NULL) | JIT backfill inside open_or_reuse_session: when actor_kind='user' with populated user_id and clients.auth_user_id IS NULL, do a single raw SELECT on auth.users.email; if it matches clients.email case-insensitively, UPDATE clients SET auth_user_id=:user_id AND upsert public.profiles(id=:user_id, role='client') ON CONFLICT DO NOTHING in the SAME transaction. Any failure (permission denial, mismatch, NULL row) returns False — auth_user_id stays NULL and the caller falls through to the existing FORBIDDEN/404 (D015 collapsed shape). Exactly one agent.auth.client_backfilled INFO log carries session_id/client_id/user_id — never email, never name. | A redeem-time hook (trigger or callback) would have needed new migration + new security surface + a second moving part. The JIT pattern keeps state transitions inside the single existing service transaction that already owns the auth grid for this route, fails closed on any grant drift, and collapses all "not-yet-matchable" cases to the same D015 404 response the rest of the stack already speaks. It also keeps the profiles row in lockstep with auth_user_id so _actor_for_user stays consistent on every subsequent request. Alternative (an explicit /auth/link endpoint called from the web side after magic-link) would have leaked existence via an extra response surface. | Yes — revisit if we ever add a redeem-time trigger that backfills auth_user_id at consume time (would make this a no-op), or if the auth.users cross-schema read gets rate-limited or grant-revoked. | agent |
| D022 | M001/S08/T03 — resolved while designing the queue replay semantics for the advisor-release path. | pattern | How the agent-write queue replay preserves the uniform lock-gate invariant | Advisor's `release_lock` clears `itineraries.locked_by` BEFORE `drain_queue` runs. Drain iterates the in-memory FIFO and calls the ordinary service-layer mutation (e.g. `add_node`) per entry; the natural `_check_lock` pass inside that mutation now admits the agent write because the row is unlocked. No `_skip_lock_check=True` flag, no parallel code path — every mutation flows through the same gate. | Threading a bypass flag through `add_node`/`update_node`/`add_edge` would fork the service layer into two code paths (gated vs. bypass) and invite future mutations to drift. Clearing the lock first + replaying through the normal path keeps the lock gate the one source of truth for "who may write" and keeps every replayed mutation observationally identical to a same-actor write landing moments later. Per-entry failures still log WARNING + continue (single poison payload can never stall the release path). | Yes — revisit if a future slice needs to replay mutations while the lock is still held (e.g. a checkpoint/commit operation that leaves the advisor locked in for further edits). | agent |
| D023 | M001/S08/T04 — resolved when wiring the draft-read gate on `GET /itinerary/{id}`. | security | Who can GET a draft-status itinerary | Admit an advisor (via `_is_requester_advisor` profiles SELECT), the owning client (`itinerary.client_id == actor.user_id`), or the creator (`itinerary.created_by == actor.user_id`); anyone else gets 403 `{"detail": "forbidden"}` with an `itinerary.draft_access_denied` INFO log carrying only an 8-char sub hint + itinerary_id. Approved itineraries skip the gate entirely. | The plan explicitly names advisor + owning-client. The creator-match extension fell out of preserving the "I can read my own draft" path that every S02 acceptance test and future dev/test caller depends on — without it, the gate would break existing callers whose subs aren't wired through the advisor profile. The three-way OR keeps the human intent simple to audit (advisor sees all drafts; client sees only their own once approved; creator sees their own work), and existence is still not leaked — the 403 shape is identical for "exists but you can't read" and "doesn't exist but you're not allowed to fish" since upstream callers probe through auth-gated routes. | Yes — tighten or broaden based on future multi-advisor collaboration (R058) or handoff semantics. | agent |
| D024 | MVP/M002-B2 — raised by founder while reviewing the Duffel flights provider; shapes B4 / M004-G1 / M005. | data / pattern | How booking state and the repriceable supplier-offer lifecycle are modeled | Two structured tables, separate from node status: `node_offers` (transient, time-boxed supplier quotes attached while a node is pre-booking — amount + currency + `priced_at` + `expires_at` + refresh lineage + raw supplier payload) and `bookings` (the committed booking record — supplier order id / PNR, the amount actually charged, `booked_by`/`booked_at`, change-cancel terms, and FKs to the offer it was booked from and the covering invoice line item). `node_status` stays the lifecycle axis (idea→proposed→approved→booked→confirmed); booking/money detail lives in these tables, never in `metadata`. Flight offers are treated as quotes valid only until `expires_at` and MUST be re-priced before `approved → booked`; the money gate reconciles against the re-priced / held amount, and any price delta is surfaced for advisor/traveler confirmation. Draft schema in `mvp-plan.md` §8; forward decision **D-BOOK** in `mvp.md` §6. | A Duffel flight offer is a price HELD for a fixed window, not a stable listing — it goes stale and reprices on refresh, so a single static node cost (B4) can't represent it, and the M005 money-gate invariant (Σ paid invoice lines ⇔ Σ booked node costs) breaks if cost is assumed stable between invoice-issue and booking. Free-text `metadata.snapshot` cannot carry orders/PNR/transactions or enforce one-active-quote / one-live-booking-per-node. Structured tables make offer expiry, refresh history, re-price delta, and the booking↔invoice↔payment links first-class and queryable, and keep booked-node immutability (G1) backed by real data. Mirrors D005 (graph state is relational + append-only, not JSONB blobs). | Yes — column set is a draft; refined when B4/M005 are built. | collaborative |
| D025 | MVP/M005-I2 — locked by founder before building the money slice (D-PAY). | data / pattern / security | Payment gateway + the money gate (pay-before-book) | **Braintree** (Python SDK, **sandbox** for the MVP) ported from the `voyage-site` Node reference (`brainTreeGateway` + `/token` + `transaction.sale`), behind a **gateway-agnostic** `PaymentGateway` protocol + normalized `SaleResult` so a future processor (Stripe) is one new impl, not a rewrite. Payments persist a `payments` row per attempt: OUR `gateway_reference` (also written to the processor side as its order id — two-way cross-reference) separate from the processor's `processor_transaction_id`, normalized portable columns (instrument / last-four / response code), and a polymorphic `raw` jsonb catch-all. The **money gate** (I3): a node may go `approved → booked` only when a covering **paid** invoice line exists; an advisor may override to a merely **issued** line, but the override is **logged**. Invoices are a signed, append-only-after-issue **ledger** (charges/discounts/adjustments/taxes/fees + reversal voids); `total = Σ lines` is the single source of truth the reconciliation invariant checks. Local/CI/e2e use a deterministic **FakeGateway** (no credentials); production without keys **refuses** (`payments_unconfigured`) rather than silently faking a charge. | Braintree has an in-house reference impl (lower risk than Stripe-from-scratch) and a working sandbox; gating `booked` on a *paid* invoice line makes "pay before book" a DB invariant, not a convention. The gateway-agnostic shape + polymorphic `raw` keep us from being locked to Braintree and from losing processor state. The signed ledger (vs. a flat node-price list) is what real invoicing needs — discounts, child/elderly adjustments, and journal-entry voids — and keeps the money-gate reconciliation honest. Mirrors D005 (relational + append-only) and D024 (booking detail lives in structured tables, not `metadata`). | Yes — swap the gateway impl, add a refund flow, or tighten the override-to-issued policy as the booking workflow (I3) matures. | human |
| D026 | MVP/M005-I3 — settled while building the money gate + booking workflow; finalizes D024's draft schema within the D025 envelope. | data / pattern | The concrete money-gate, re-price, and reconciliation semantics (booking workflow) | **Tables (migration 0025, finalizing D024's draft):** `node_offers` (source `duffel`/`snapshot`/`manual`, amount/currency, `priced_at`/`expires_at`, `refreshed_from_offer_id` lineage, raw payload) + `bookings` (amount charged, FK→offer, FK→covering `invoice_line_item`, `supplier_ref`, `override_unpaid`, `booked_by/at`, `confirmed_at`; UNIQUE(node_id) = one live booking per node). **Money gate (`services/bookings.book_node`):** a node is *covered* when Σ(its invoice lines on **paid** invoices, same currency) ≥ the booked amount; with `override_unpaid` an advisor may instead cover from a merely **issued** invoice (recorded on the booking + WARN-logged). The booked amount is the node's B4 cost for a non-flight, or the latest **fresh** (non-expired) offer for a `flight` (else refused `offer_required`/`offer_expired`). **Live re-price** reuses the existing provider `get_detail` (Duffel `GET /air/offers/{id}`) — no new provider method; a `snapshot` fallback freezes B4 cost (+30-min window) where no provider source exists, so the flow runs offline. **Bypass closed:** `update_node` refuses a direct `→booked`/`→confirmed` flip (`use_booking_flow`, new `CONFLICT`→409 outcome); the gate is the only path in. **Reconciliation** (`reconcile_itinerary`, surfaced at `GET /itinerary/{id}/reconciliation` + the `ovb.invariants.money_gate_reconciles` invariant): per-currency Σ(paid lines for booked nodes) vs Σ(booking amounts), with per-node violations (`booked_unpaid`/`undercharged`/`overcharged`). | D024 left the column set a draft and D025 the gate to "as I3 matures"; building it forced the precise shapes. Reusing `get_detail` for re-price (it already returns current `total_amount`+`expires_at`, 404 on lapse) avoids a parallel provider surface; the snapshot fallback keeps CI/e2e deterministic while the live Duffel path is real when configured. Closing the `update_node` bypass makes "pay before book" a true invariant (mirrors D022 — one write path, no bypass flag) rather than a convention an advisor could sidestep with a status edit. Linking each booking to its covering line + surfacing per-node deltas keeps reconciliation honest under re-price/void. | Yes — tighten the override-to-issued policy, add party-size expansion to coverage, or wire a refund/cancel flow as booking matures. | agent |
| D027 | MVP — retires the invite-code surface from D014/D015/S01. | arch / security | Drop invite codes — email magic-link is the only front door | The `invites` table, `/auth/redeem-invite`, the invite-code/reissue/cancel lifecycle, and the login-page code form are removed (migration `0026_drop_invites`). Adding a client (`POST /clients`) still provisions the client's Supabase auth row and emails a **code-free welcome sign-in link** (Supabase `inviteUserByEmail`, no `data.invite_code`); every sign-in after that is the existing `POST /auth/login` magic-link flow. A client's advisor-visible state is derived from `clients.auth_user_id` — `access_status` `pending`→`active` — with a new `clients.accepted_at` (migration `0027`, stamped at first-login backfill) so the advisor sees *if/when* they signed in. The advisor "nudge" re-sends the welcome link (`POST /clients/{id}/resend-welcome`, refuses 409 once accepted). All auth email is relayed by **Resend** as the Supabase Auth SMTP (configured on the remote project; apps/api sends no email and holds no Resend key). | The invite code was redundant friction: a fully working email magic-link path (`/auth/login` + `SignInEntry`) already existed in parallel, and the code added a table, a public endpoint, a lifecycle, and a second login form for no security gain (the email link is itself the bearer credential). Deriving status from `auth_user_id`/`accepted_at` removes the `invites` table while keeping the useful advisor signals (pending vs active, first-login time, re-send). Resend-as-SMTP keeps email delivery in Supabase (where it already lived) so no app email code or new ECS secret is needed — Supabase's built-in mailer is test-only/rate-limited, so a real relay was required for staging regardless. Retires D014 (single-use consume) and D015 (redeem error-disclosure) — there is no code to redeem; `/auth/login`'s 204-collapse carries the enumeration guarantee forward. | Yes — reintroduce a code/approval step (e.g. behind a CAPTCHA) if invitation gating ever needs to be enforced server-side rather than by who-knows-the-email. | collaborative |
| D028 | MVP/Wave E (ADV-16/17) — founder calls 2026-07-08, built alongside the retime spine. | data / pattern | Relative days → pinned dates: booking gates on pinned dates; Day-N is anchored, not derived; fuzzy ↔ precise is symmetric | **No dual relative/absolute node schema** — nodes keep absolute `starts_at`, made honest by an explicit itinerary `days_anchor` (migration `0041`; Day N ≡ days_anchor + (N−1), stamped when the first card is scheduled: the window's `date_start`, else the current UTC date). **Pinning is a server-side shift**: `POST /itinerary/{id}/retime` computes `delta = new date_start − days_anchor`, shifts every scheduled node's `starts_at`/`metadata.start_time` by whole days preserving wall-clock + tz offset (node_history per move), flips `timing_kind=exact`, re-stamps the anchor. **Booking is structurally impossible until pinned**: `book_node`/`record_confirmation` refuse with 409 `dates_not_pinned` unless `timing_kind=exact`; the reverse guard refuses loosening (`exact`→`window`/`flexible`) and any non-zero retime while booked/confirmed cards exist (409 `booked_dates_locked`). **Unpin moves nothing**: cards stay put, the anchor is kept, labels flip back to Day-N — so fuzzy → pinned → fuzzy → re-pinned round-trips as uniform shifts. **Strictly Day-N until pinned** on every surface (day tiles, card when-stamps, invoice `NodeIdentity`): no weekday/date sub-lines, no tentative dates — a shared `formatNodeWhen`/`dayOrdinal` helper renders "Day 3" or hides when nothing anchors Day 1. Demo seeds (Japan template + live) are born pinned since they materialize concrete dates. | A `day_offset` column would fork every read path (timeline, linearization, analyze, Fill, invoice when-stamps, agent context); the codebase was already absolute-under / relative-on-top — this makes the pattern honest instead of accidental (the old anchor was "the day the advisor happened to build", renumbering days when the Day-1 card was deleted). Gating booking on `exact` makes the retime ↔ booked-card conflict structurally impossible rather than a policy to arbitrate (founder: pre-invariant data will be wiped, so no legacy path). A wrong-but-plausible fabricated date is worse than an honest ordinal (founder call). Precedent: template nodes already do offset-from-anchor → materialize-absolute (0015). | Yes — partial pinning ("flights fixed, the middle flexible") is consciously out; revisit if whole-trip pinning proves too coarse. | collaborative |
| D029 | MVP/Wave F (Mission Control) — 2026-07-09. | arch / pattern | Advisor live-update transport for the Command Center | **Stateless poll-to-push SSE** — `GET /advisor/feed` holds one generator per connection that ticks every `FEED_POLL_SECONDS=4`, re-running the `/advisor/activity` loader in `after=watermark` **ascending** mode with a short DB session per tick (the `stream_turn` sessionmaker pattern), and emits each projected `ActivityEvent` as a `data:`-only JSON frame. Heartbeats ~20s when idle (the ALB idle timeout is 120s); the stream closes with `bye{reason:"reauth"}` at min(JWT exp, `FEED_MAX_STREAM_SECONDS=900`); `?cursor=` resumes and omitting it starts at now(). The server holds **nothing** across connections — restart, deploy, and multi-task all reduce to "client reconnects, snapshots via refresh, resumes from cursor". Browser side is a fetch-reader (EventSource cannot send Authorization) with heartbeat watchdog, backoff + jitter, degrade-to-polling after 4 failures, and a 15s visible-only `router.refresh()` throttle (force-dynamic pages re-run their whole fetch set per refresh). | Reuses the activity projection verbatim — one loader is the single truth for the feed, the roster page, and the SSE tick, so the live stream can never disagree with the page it updates. Stateless per-connection polling needs no pub/sub infrastructure, no sticky sessions, and no cross-task coordination on ECS; a 4s indexed keyset query per connected advisor is negligible at boutique scale (single-digit advisors). Postgres LISTEN/NOTIFY was considered and deferred: Supabase's connection pooler makes it fragile, and the upgrade path is swapping the tick trigger only — the frame contract, cursor resume, and client transport all stay. A notifications table was rejected as parallel state the graph/history already carry (derived-first taste, same as ADV-14). | Yes — swap the 4s tick for LISTEN/NOTIFY or a queue if advisor count or event-latency requirements ever earn it; the frame contract and client are transport-agnostic. | collaborative |
