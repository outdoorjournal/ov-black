"""ovb CLI — AWS-style command groups over the SDK, rendered with rich.

``--json`` (global) flips any command from rich output to machine-readable JSON,
so the same command serves a human operator and an agent driving the tool.
"""
