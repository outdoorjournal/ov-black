import { Shell } from "./_components/Shell";

export const metadata = {
  title: "Cards · OV Black prototype",
  description:
    "Design sandbox for OV Black itinerary cards: type taxonomy, status states, glance + zoom views per type, and accessibility notes.",
};

export default function CardsPrototypePage() {
  return <Shell />;
}
